// Auto-generated by i18n CLI - do not edit manually
// Generated at: 2025-09-24T10:13:45.185Z

export const dictionaries = {
  "en": {
    "hello": "Hello",
    "bye": "Bye",
    "example": {
      "hello": "Hello"
    },
    "items": {
      "count": (count) => {
      const forms = {"one":"item","other":"items"};
      const lang = "en"; // This will be replaced by the build system
      const pluralRules = new Intl.PluralRules(lang);
      const rule = pluralRules.select(count);
      
      // Direct object property access - no CLDR ordering needed!
      if (forms[rule]) {
        return forms[rule];
      }
      
      // Fallback priority: other > one > first available
      return forms.other || forms.one || Object.values(forms)[0] || '';
    }
    },
    "product": {
      "count": (count) => {
      const forms = {"one":"product","other":"products"};
      const lang = "en"; // This will be replaced by the build system
      const pluralRules = new Intl.PluralRules(lang);
      const rule = pluralRules.select(count);
      
      // Direct object property access - no CLDR ordering needed!
      if (forms[rule]) {
        return forms[rule];
      }
      
      // Fallback priority: other > one > first available
      return forms.other || forms.one || Object.values(forms)[0] || '';
    }
    },
    "test": {
      "cars": (count) => {
      const forms = {"one":"car","other":"cars"};
      const lang = "en"; // This will be replaced by the build system
      const pluralRules = new Intl.PluralRules(lang);
      const rule = pluralRules.select(count);
      
      // Direct object property access - no CLDR ordering needed!
      if (forms[rule]) {
        return forms[rule];
      }
      
      // Fallback priority: other > one > first available
      return forms.other || forms.one || Object.values(forms)[0] || '';
    }
    }
  },
  "fr": {
    "hello": "Bonjour",
    "bye": "Au revoir",
    "example": {
      "hello": "Bonjour"
    },
    "items": {
      "count": (count) => {
      const forms = {"one":"article","other":"articles"};
      const lang = "fr"; // This will be replaced by the build system
      const pluralRules = new Intl.PluralRules(lang);
      const rule = pluralRules.select(count);
      
      // Direct object property access - no CLDR ordering needed!
      if (forms[rule]) {
        return forms[rule];
      }
      
      // Fallback priority: other > one > first available
      return forms.other || forms.one || Object.values(forms)[0] || '';
    }
    },
    "product": {
      "count": (count) => {
      const forms = {"one":"produit","other":"produits"};
      const lang = "fr"; // This will be replaced by the build system
      const pluralRules = new Intl.PluralRules(lang);
      const rule = pluralRules.select(count);
      
      // Direct object property access - no CLDR ordering needed!
      if (forms[rule]) {
        return forms[rule];
      }
      
      // Fallback priority: other > one > first available
      return forms.other || forms.one || Object.values(forms)[0] || '';
    }
    },
    "test": {
      "cars": (count) => {
      const forms = {"one":"voiture","other":"voitures"};
      const lang = "fr"; // This will be replaced by the build system
      const pluralRules = new Intl.PluralRules(lang);
      const rule = pluralRules.select(count);
      
      // Direct object property access - no CLDR ordering needed!
      if (forms[rule]) {
        return forms[rule];
      }
      
      // Fallback priority: other > one > first available
      return forms.other || forms.one || Object.values(forms)[0] || '';
    }
    }
  },
  "ru": {
    "hello": "Привет",
    "bye": "Пока",
    "example": {
      "hello": "Привет"
    },
    "product": {
      "count": (count) => {
      const forms = {"one":"товар","few":"товара","many":"товаров","other":"товаров"};
      const lang = "ru"; // This will be replaced by the build system
      const pluralRules = new Intl.PluralRules(lang);
      const rule = pluralRules.select(count);
      
      // Direct object property access - no CLDR ordering needed!
      if (forms[rule]) {
        return forms[rule];
      }
      
      // Fallback priority: other > one > first available
      return forms.other || forms.one || Object.values(forms)[0] || '';
    }
    },
    "test": {
      "cars": (count) => {
      const forms = {"one":"машина","few":"машины","many":"машин","other":"машин"};
      const lang = "ru"; // This will be replaced by the build system
      const pluralRules = new Intl.PluralRules(lang);
      const rule = pluralRules.select(count);
      
      // Direct object property access - no CLDR ordering needed!
      if (forms[rule]) {
        return forms[rule];
      }
      
      // Fallback priority: other > one > first available
      return forms.other || forms.one || Object.values(forms)[0] || '';
    }
    }
  }
};

export const languages = ["en","fr","ru"];
