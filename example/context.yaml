context: This is a test project about internationalization
inputs:
  items:
    count:
      input: item
      context: things you can count
    message:
      input: message
      context: communication unit
    notification:
      input: notification
      context: system alert
  product:
    count:
      input: product
      context: item for sale
  buttons:
    catalog:
      input: Перейти в каталог
      context: или открыть, посмотреть, как принято в соответствующем языке
  test:
    input: weeks
  test/moved:
    key:
      input: Main key
mounts:
  bar: baz
