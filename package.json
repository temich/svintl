{
  "name": "svintl",
  "version": "0.1.9",
  "publishConfig": {
    "access": "public"
  },
  "description": "Internationalization for Svelte",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/temich/svintl.git"
  },
  "homepage": "git@github.com:temich/svintl#readme",
  "type": "module",
  "main": "transpiled/index.js",
  "types": "transpiled/index.d.ts",
  "bin": {
    "intl": "bin/intl.cjs"
  },
  "files": [
    "transpiled/**/*",
    "bin/**/*",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "build:cli": "esbuild source/cli/index.ts --bundle --platform=node --format=cjs --target=node22 --outfile=bin/intl.cjs",
    "prepare": "husky",
    "prepublishOnly": "rm -rf transpiled tsconfig.tsbuildinfo && npm run build && npm run build:cli",
    "lint": "eslint .",
    "format": "eslint . --fix",
    "test": "npm run lint && node --test",
    "bump": "npx -y npm-check-updates --target minor -u && npm install"
  },
  "peerDependencies": {
    "svelte": "*",
    "typescript": "*"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.3.0",
    "esbuild": "^0.25.9",
    "eslint": "^9.33.0",
    "eslint-plugin-import": "^2.32.0",
    "husky": "^9.1.7",
    "neostandard": "^0.12.2"
  },
  "dependencies": {
    "dotenv": "^17",
    "js-yaml": "^4",
    "openai": "^5"
  }
}
